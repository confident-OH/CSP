#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
    /** vector初始化
     *  vetctor<type> v(100, 0) : 类型为type, 容量为100，每个元素初始化为0的vector数组
     *
     */
    /** vector常用：push_back(), pop_back(), size(), erase(), insert()
     *  vector指针：v.begin(), v.end()
     *
     */


    return 0;
}
